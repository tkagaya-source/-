# 千葉日本大学第一中学校・高等学校 教職員ポータル

## プロジェクト概要

私立中高一貫校（千葉日本大学第一中学校・高等学校）の教職員向けポータルサイト。
連絡事項の管理・行事カレンダー・各種申請リンク・共有ドキュメント・出勤状況表示などの機能を持つ。
フレームワーク不使用のHTML/CSS/JavaScriptで構成される静的サイト。

## ディレクトリ構成

```
/
├── index.html          # メインページ（エントリーポイント）
├── css/
│   └── style.css       # スタイルシート（CSS変数・BEM命名規則）
├── js/
│   ├── data.js         # サンプルデータ定義（お知らせ・行事・共有リンク・出勤状況）
│   └── app.js          # アプリケーションロジック（描画・カレンダー・ナビゲーション）
└── CLAUDE.md           # このファイル
```

## 技術スタック

- **HTML5**: セマンティック要素（header, main, nav, section, article, footer）
- **CSS3**: CSS変数（カスタムプロパティ）、Flexbox、Grid、メディアクエリ
- **JavaScript (ES6+)**: IIFE + strict mode、const/let、アロー関数、テンプレートリテラル
- **Webフォント**: Google Fonts（Noto Sans JP, Poppins）

ビルドツール、パッケージマネージャー、テストフレームワーク、リンター、CI/CDは一切使用していない。

## 開発方法

### ローカルでの確認

ビルドステップは不要。`index.html` をブラウザで直接開くか、任意のHTTPサーバーで配信する。

```sh
# Python 3 の場合
python3 -m http.server 8000

# Node.js (npx) の場合
npx serve .
```

### コーディング規約

- **HTML**: セマンティックな要素を使用。BEM風のクラス命名（`block__element--modifier`）
- **CSS**: CSS変数で色・余白・角丸等を管理。コンポーネント単位でセクション分け。`/* ===== セクション名 ===== */` コメントで区切り
- **JavaScript**: IIFE でグローバル汚染防止。`"use strict"` 使用。動的HTML挿入時は必ず `escapeHtml()` でXSS対策
- **データファイル**: `js/data.js` でグローバル変数としてデータ定義

### 言語

UIテキストはすべて日本語。コード中のコメントも日本語で記述する。

## 機能一覧

| 機能 | 説明 |
|------|------|
| お知らせ | カテゴリ（重要・NEW・連絡）付きの連絡事項一覧。フィルタ機能・詳細モーダル付き |
| 行事カレンダー | 月送り可能なカレンダー。行事のある日にドット表示。日付クリックで行事詳細表示 |
| 直近の行事 | 今後の行事を時系列で一覧表示 |
| クイックアクセス | 各種申請フォーム（休暇・施設・備品）へのショートカット |
| 共有ドキュメント | 成績管理・授業計画・年間行事計画等へのリンク集 |
| 今日の状況 | 出勤・出張・休暇の人数表示 |
| ナビゲーション | タブ切替によるセクション表示制御 |
| 挨拶表示 | 時間帯に応じた挨拶メッセージ（おはよう/こんにちは/お疲れさま） |

## データ構造（js/data.js）

### お知らせ (`announcements`)

```js
{
  id: 1,
  date: "YYYY/MM/DD",
  category: "important" | "new" | "info",
  categoryLabel: "重要" | "NEW" | "連絡",
  department: "教務部",
  title: "タイトル",
  body: "本文"
}
```

### 行事 (`events`)

```js
{
  id: 1,
  date: "YYYY-MM-DD",
  title: "行事名",
  detail: "詳細（時間・場所等）",
  color: "#hex"
}
```

### 共有リンク (`sharedLinks`)・出勤状況 (`staffStatus`)

データ構造はjs/data.jsを参照。

## セキュリティ

- HTMLに動的コンテンツを挿入する際は **必ず** `escapeHtml()` を通す（XSS対策）
- `escapeHtml()` は `&`, `<`, `>`, `"`, `'` の5文字をエスケープする

## 変更時の注意

- 新しいお知らせカテゴリを追加する場合は `css/style.css` に対応する `.badge-<name>` クラスを追加する
- 行事を追加する場合は `js/data.js` の `events` 配列に追加する
- HTMLに動的コンテンツを挿入する際は必ず `escapeHtml()` を通す
- スクリプトの読み込み順序（data.js → app.js）を変更しないこと
- CSS変数は `:root` で一元管理し、直値のハードコードを避ける
